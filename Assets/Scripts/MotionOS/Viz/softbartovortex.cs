using UnityEngine;
using System.Collections;

public class softbartovortex : MonoBehaviour {
	public NITESoftbar softbar;
	//public VortexEffect vortex; // COMMENTED OUT UNTIL WE ALL HAVE PRO
	public float maxAngle = 720f;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//vortex.angle = softbar.Value * maxAngle; // COMMENTED OUT UNTIL WE ALL HAVE PRO
	}
}
